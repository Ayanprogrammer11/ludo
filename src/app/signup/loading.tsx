import { RouteLoading } from "@/components/loading/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Join the table" title="Loading account setup..." detail="Preparing the secure account form." />;
}
