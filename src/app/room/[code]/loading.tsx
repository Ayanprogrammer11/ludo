import { RouteLoading } from "@/components/loading/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Private room" title="Opening the table..." detail="Restoring the latest room state from the server." />;
}
