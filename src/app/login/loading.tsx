import { RouteLoading } from "@/components/loading/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Account" title="Loading sign in..." detail="Checking your current session." />;
}
